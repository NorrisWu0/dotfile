# Add personal app-launching keybindings here.
# Core tiling, window, workspace, and utility bindings are provided by omarchy defaults.
#
# Examples:
# bind = SUPER, T, exec, omarchy-launch-tui btop
# bindd = SUPER SHIFT, S, Screenshot, exec, omarchy-capture-screenshot
